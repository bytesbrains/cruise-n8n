# Brand assets

| File | Use |
| --- | --- |
| `cruise-logo-480.png` | Local copy of the product wordmark (same file as `https://bytesbrains.com/brand/cruise-logo-480.png`) |

The README embeds the CDN URL so npmjs.com and GitHub render the same mark. Prefer the CDN in
docs; keep this file so contributors can work offline and so the source of the mark is in-repo.
